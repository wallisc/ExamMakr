package qsolutions.api;

/**
 * The SectionTitle interface for use with LookUp. Only contains methods needed
 * externally.
 * @author Ryan Dollahon
 */
public interface SectionTitleApi extends DesignItemApi
{
    /**
     * Generates a new instance of a SectionTitle item.
     * @return the new TrueFalse
     */
    SectionTitleApi newItem();
}
